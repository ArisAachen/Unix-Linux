//
// Created by aris on 2021/6/29.
//

#include <stdio.h>

int main() {
    // test for
    int level = 0;
    int index = 0;
    for (index = 0; index < 10; ({while (level < 10) {
        printf("level is %d", level);
    }});
    level = 0;
    index++) {
        printf("index is %d", index);
    }

    return 0;
}